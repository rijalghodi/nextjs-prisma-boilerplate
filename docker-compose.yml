services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    env_file:
      - .env
    ports:
      - "3000:3000"
    environment:
      - DB_HOST=db
    depends_on:
      - db

volumes:
  pgdata:
